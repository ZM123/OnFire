version: '3'
services:
  web:
    build: ./web
    ports:
    - "8000:8000"
  worker:
    build: ./worker
    ports:
    - "8200:8200"
  backup:
    build: ./backup
  redis:
    image: "redis:alpine"
    ports:
    - "6379:6379"
